<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hegdeapps.mathz">

    <application
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
       <!-- <activity android:name=".MathQuiz"
            android:configChanges="screenSize|orientation"></activity>-->
        <activity
            android:name=".MathQuiz"

             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".GridSUM" android:configChanges="screenSize|orientation">

        </activity>-->
    </application>

</manifest>